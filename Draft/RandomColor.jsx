import React from "react";
import Wrapper from "../Styles/Wrapper";
import { Card } from "styled-bootstrap-components";

export default props => (
  <div>
    <Card width="40%" m={2} p={2}>
      Hello, World!
    </Card>
  </div>
);
